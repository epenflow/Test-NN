import { LoginForm } from '@/components/login/login-form';

export default function LoginPage() {
	return (
		<main className="container flex items-center justify-center h-svh">
			<LoginForm />
		</main>
	);
}
